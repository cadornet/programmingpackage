if (!require("BiocManager", quietly = TRUE))
install.packages("BiocManager")
BiocManager::install("clusterProfiler")
setwd("~/programmingpackage")
# Tissue origin inference based on SingleR labels and clusters
# Load required packages
library(Seurat)
library(ggplot2)
# Extract cluster and cell type info
cluster_ids <- Idents(seurat_obj)
# Tissue origin inference based on SingleR labels and clusters
# Load required packages
library(Seurat)
library(ggplot2)
# Extract cluster and cell type info
cluster_ids <- Idents(seurat_obj)
